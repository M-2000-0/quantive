{
  "name": "SVC-3: Execution Logger (manual)",
  "active": false,
  "nodes": [
    {
      "parameters": {},
      "id": "auto-Manual-0000",
      "name": "Manual",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const it = $input.first().json;\nreturn [{ json: {\n  workflow_id: it.workflowId || it.workflow_id || 'unknown',\n  workflow_name: it.workflowName || it.workflow_name || 'unknown',\n  execution_id: it.executionId || it.execution_id || 'manual',\n  status: it.status || 'success',\n  duration_ms: it.durationMs || it.duration_ms || 0,\n  metadata: JSON.stringify(it.metadata || {}),\n}}];"
      },
      "id": "auto-Build Log Row-0006",
      "name": "Build Log Row",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        0
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO workflows.workflow_executions (workflow_id, workflow_name, execution_id, status, duration_ms, metadata)\nVALUES ('{{ $json.workflow_id }}', '{{ $json.workflow_name }}', '{{ $json.execution_id }}', '{{ $json.status }}', {{ $json.duration_ms }}, '{{ $json.metadata }}'::jsonb);",
        "options": {}
      },
      "id": "auto-Insert Execution Log-0007",
      "name": "Insert Execution Log",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.4,
      "position": [
        400,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "postgres-main",
          "name": "Quantive DB"
        }
      }
    }
  ],
  "connections": {
    "Manual": {
      "main": [
        [
          {
            "node": "Build Log Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Log Row": {
      "main": [
        [
          {
            "node": "Insert Execution Log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "pinData": {},
  "versionId": null,
  "tags": [
    "service",
    "logging"
  ],
  "description": "Execution logger: writes a row into workflows.workflow_executions via the native Postgres node."
}