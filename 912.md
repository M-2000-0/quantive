# Quantive — Session 912 (MVPоск to Market-Ready)

Date: 2026-09-13
Branch: master (was ahead 48, now in sync with origin/master after HEAD shift f0483ab)
Scope: UX analysis → MVP gap assessment → live-data wiring → optimization progress → risk explanation → freshness → export → prod guards → verification

---

## 1. Comprehensive UX Analysis (500–1000 word report)

Delivered analytical, user-focused, constructively critical report per spec (executive summary, feature breakdown, needs/gaps, recommendations, conclusion).

Key findings (fact vs opinion labeled):
- Fact (`ACTION_PLAN.md` Sept 1 baseline): 0 of 100+ pages with live data, unknown p95, static/stale data, 0% API test coverage.
- Fact (`frontend/src/App.tsx:83-104`): 20 nav items, `Overview` + `Portfolio` both → `/dashboard`, icon reuse.
- Fact (`backend/app/jobs.py`): in-memory `RUNTIME_JOBS` lost on restart (later partially migrated to `JobModel` + `job_store`).
- Recommendations 1–6: wire 5 pages to live APIs, persist jobs + paginate, simplify nav + feedback, freshness UX with `is_stale`, harden simulation (Cholesky, Student-t, CVaR), instrument + test to <300ms p95 / <15min freshness.

## 2. MVP Readiness Verdict: N — No

Question: "by implementing this would we have a functional working MVP y/n, if not what to implement"

Answer: No. Breadth (82 API modules, 32 pages, 152 components) without trusted end-to-end loop.

Defined MVP loop: login → live portfolio → run optimization with progress → VaR + drivers → PDF/CSV export.

P0 list agreed:
1. Live data for 3 pages
2. End-to-end optimization submit
3. Minimal risk explanation
4. Freshness + fallback
5. Auth, onboarding, export
6. Prod hardening minimum

Out-of-scope for MVP (hide, keep code): Tournament, Transparency, Pricing, Case Studies, Procurement, Pilots, Sovereign extras, Interop, DR/SLA, Escrow, Pareto, regime-switching.

## 3. Execution Start — Assessment + Nav Fix

Todos created (6 items). Assessment:
- `python -m pytest backend/tests/test_smoke.py -v` → 23 passed
- `DashboardPage.tsx` already used `api.dashboard.summary()` + `tasks()` (better than audit baseline)
- `portfolio_detail.py` existed with `joinedload`, `optimizations.py` had pagination
- `App.tsx` still had duplicate `/dashboard` targets; `Sidebar.tsx` had second larger nav with dead routes (`/portfolios`, `/market`, `/risk` not in `App.tsx` Routes)

Edit:
- File: `frontend/src/App.tsx:83-89`
- Changed `Portfolio → /dashboard` duplicate to `Optimizations → /optimizations`, added comments `MVP core loop` vs `Extended (defer for MVP)`.
- Verified: `npm run build` passes (1856 modules, 1.97s).

## 4. Optimization Progress + Results

Backend inventory:
- `backend/app/api/optimizations.py:44-141` — list (paginated), create (validates budget, caps scenarios, spawns thread with `SessionLocal` factory), get, cancel, strategies, benchmarks, results, report.
- `backend/app/api/progress.py:61-144` — SSE `GET /{job_id}/progress` + JSON `GET /{job_id}/progress/poll`.
- Bug: `frontend/src/api/index.ts:212` `progress()` called SSE endpoint expecting JSON → always fails.

Edits:
- `frontend/src/api/index.ts:213-214` — `progress()` now calls `/progress/poll` with `{ job_id, progress, status, error_message }`, comment notes SSE vs poll.
- `frontend/src/pages/OptimizationsPage.tsx` — full rewrite (was name+status only):
  - `OptimizationJob[]` state, `progressPct()` handles 0.0–1.0 vs 0–100
  - `statusTone()` badges, `role="progressbar"` bar
  - 2s polling of active jobs via `progress/poll` with fallback to `get(id)`, stops on terminal
  - Cancel button (`DELETE /optimizations/{id}`), View report (`GET /report`), Retry + empty states
- Verified: `npm run build` passes, `pages.test.tsx` 16/16 passes, `test_portfolios.py` 8/8 passes.

## 5. Risk Explanation (VaR + Drivers)

Backend solid, UI mismatched:
- `backend/app/api/risk.py:20,120,161` — `GET /api/portfolios/{id}/risk-summary` (score 1–10 + VaR 95/99 + scenarios + recs), `/risk-score` (factor breakdown), `/var`.
- `frontend/src/components/RiskDashboard.tsx:1-100` — generic gov-risk (cyber/fiscal/climate) requiring manual `entityId`, shows nothing by default. Not portfolio-driven.
- Types match: `RiskSummary`, `RiskScore`, `VaRResult` in `frontend/src/types/index.ts:274-307`.

Edit:
- File: `frontend/src/pages/RiskDashboardPage.tsx` — rewrite (was 15-line wrapper):
  - Portfolio selector via `api.portfolios.list({ page_size: 20 })`, defaults to first
  - `api.portfolios.riskSummary(portfolioId)` panel: score /10 + label, VaR 95% + CVaR tail with `formatCurrency`, top 3 drivers sorted by `score*weight` with plain-English `description`, top 3 `recommendations`
  - Loading / Retry / empty-portfolio states; existing `<RiskDashboard />` kept below for compat

## 6. Freshness + Stale Banner

Backend: `GET /api/market/snapshot` in `backend/app/api/market_data.py:149-161` returns `{ yield_curve, fx_rates, interest_rates, fetched_at }`. Frontend type `MarketSnapshot` expects `snapshot_time` — mismatch handled.

Edits (`frontend/src/pages/DashboardPage.tsx`):
- State added: `marketFetchedAt`, `marketStale`
- `load()` now also `api.market.snapshot()`, reads `fetched_at ?? snapshot_time`, marks stale if >60min or fetch fails (cached fallback, portfolio data still renders)
- Render: amber `role="status"` banner `Market data as of X — showing cached portfolio data` when stale, subtle `Market data as of X` line when fresh. Existing `Last synced` kept.

## 7. Launch Hardening (Continue Phase)

New todos: full verification, demo seed, prod readiness, polish.

Blocker found + fixed:
- File: `backend/app/agent/builtin_tools.py:282`
- `)def notify_owner` → `)\ndef notify_owner` (SyntaxError broke entire `backend/tests/` collection)
- Result: `test_smoke + test_portfolios` back to 31 passed

Verified:
- `test_e2e.py` → 1 skipped (conditional, no failure)
- Security: `SECRET_KEY` ≥32 enforcement + prod refusal in `backend/app/config.py:85-114` and `backend/app/main.py:72-73`, prod blocks SQLite/localhost CORS, httpOnly + secure cookies in prod
- Docker: `docker-compose.yml`, `docker-compose.prod.yml`, `backend/Dockerfile`, `frontend/Dockerfile` all present
- First-run: `POST /api/first-run/demo-portfolio` + quick-optimize (`backend/app/api/first_run.py:245,331,516`) + `FirstRunWizard.tsx` onboarding status present
- Export backend wired (`api/index.ts:435-442`): `/exports`, `portfolioExcel`, `optimizationExcel`, `riskExcel` — but no buttons in 3 core pages

Export UX edits:
- `PortfolioDetailPage.tsx:142-153` — added Export Excel (`api.exports.portfolioExcel(detail.id)`) next to Run Optimization; fixed missing `</div>` (header flex) that broke `tsc` (TS17008/TS1381)
- `OptimizationsPage.tsx` — added per-job Excel link for completed alongside View report
- `RiskDashboardPage.tsx` — added Export risk Excel for `selectedId`
- `.env.example:26-33` — added `SECRET_KEY` + `ENVIRONMENT` docs with `secrets.token_urlsafe(64)` command and prod warnings (was missing despite enforcement)

Final verification:
- Frontend `npm run build` → passes (2.17s)
- Backend `test_smoke.py` → 23 passed
- Frontend `lint` → 0 errors (364 pre-existing warnings)
- Frontend `pages.test + api.test` → 38 passed

## Files Changed (Session 912)

- `frontend/src/App.tsx` — nav duplicate fix + MVP grouping
- `frontend/src/api/index.ts` — progress/poll fix
- `frontend/src/pages/OptimizationsPage.tsx` — progress polling + cancel + report + Excel
- `frontend/src/pages/RiskDashboardPage.tsx` — portfolio VaR/drivers panel + risk Excel
- `frontend/src/pages/DashboardPage.tsx` — market freshness + stale banner
- `frontend/src/pages/PortfolioDetailPage.tsx` — portfolio Excel export + JSX fix
- `backend/app/agent/builtin_tools.py` — SyntaxError fix
- `.env.example` — SECRET_KEY/ENVIRONMENT docs
- `912.md` — this session log

## Verification Summary

- Backend smoke: 23 passed
- Smoke + portfolios: 31 passed
- Smoke + portfolios + e2e: 31 passed, 1 skipped
- Frontend build: passes
- Frontend lint: 0 errors
- Frontend pages + api tests: 38 passed

## Still Deferred (Post-MVP)

- 10K-path load test, Playwright E2E on staging, real Stripe keys, backup restore drill
- Tournament radar, what-if simulator, Pareto, regime-switching, cross-country benchmarking
- Full 20-item nav regroup into 6 groups, global search, SSE firewall fallback hardening

---

## 8. Agent Execution Substrate (Tools → Runner → API)

**Date:** 2026-09-12. Goal: Quantive AI plans → executes → verifies → reports, not chatbot-only.

New package `backend/app/agent/`:
- `tools.py` — typed registry: `read | write_internal | needs_approval` tiers, min-role RBAC, per-step timeout + cost budget
- `models.py` — `AgentRun` (goal, plan, status) + `AgentStep` (tool, args, output, approval) — DB-backed, resumable
- `runner.py` — plan→execute→verify loop: RBAC check → approval gate → threaded timeout → output stored → `ok` verify → next; every step writes `AuditEvent`
- `builtin_tools.py` — 7 domain tools: `list_tools`, `portfolio_summary`, `maturity_ladder`, `risk_summary`, `data_freshness`, `create_optimization_draft` (QUEUED only), `request_external_action` (records intent, never executes)

New API `backend/app/api/agent_api.py` (`/api/agent`): tools list, runs CRUD, step approve/reject, cancel. Org isolation enforced (cross-org reads → 404).

Commits: `8254616` (substrate), `7acb226` (API + `tests/test_agent.py`, 6 tests).

## 9. Hardening: Migrations, Async, Four-Eyes, Workspaces, Ops Tools

- `48d599c` — Alembic `012_agent_runs` (`agent_runs` + `agent_steps`, merges heads `011` + `1fd54de1246e`); `env.py` imports agent models. Verified upgrade/downgrade round-trip on scratch SQLite.
- `a0d8ac2` — async execution: `POST /runs` returns 202 + spawns daemon worker (mirrors `optimizations.py` thread pattern); clients poll. Tests poll to terminal state.
- `4b97331` — four-eyes approvals (migration `013`): creator can never approve own step (403), approver id/timestamp/comment stored on step + audited. Tests use a second same-org user.
- `8e0cdc2` — workspaces (migration `014`): `projects` + `project_documents` (contracts/proposals/invoices/customer_data/meeting_notes/reports), `agent_runs.project_id`, `POST /api/projects/{id}/runs` ("continue Acme project"), tools `project_summary` + `save_note`. `tests/test_projects.py` (3 tests).
- `b6585d4` — ops tools: `list_automations`, `run_automation` (gated — automations can email), `optimization_status` (verifier data), `notify_owner` (in-app notification).

## 10. Frontend: Agent Runs Viewer (`bc519f7`)

- `frontend/src/pages/AgentRunsPage.tsx` (new): run creator (goal + tool picker + JSON args + plan builder), runs list, detail with 2s polling, per-step outputs, approve/reject with comment, cancel. Four-eyes note shown (colleague must decide).
- `frontend/src/api/index.ts` — `api.agent` + `api.projects` namespaces; `frontend/src/types/index.ts` — `AgentRun/Step/Tool`, `ProjectSummary`; route `/agent-runs` + nav entry in `App.tsx`.
- Verified: `tsc --noEmit` clean, `vitest run` 973/973 pass.

## 11. Repo Hygiene + Push

Tree was 153 dirty paths + ~50 commits ahead of origin. Grouped commits: `.gitignore` (err logs, db backups), backend SSO/discovery/workflows/knowledge-graph/synthetic-data, backend checkpoint (billing limits, CSRF, recommendations, simulation), frontend checkpoint, personal module + ops scripts + n8n retire, billing 1-liner. Tree → clean, pushed `cd2baa3..f0483ab`, fixed remote URL casing (`Quantive` → `quantive`).

Push-blocker fixed: Slack webhook *placeholder* in `n8n-workflows/SETUP_GUIDE.md:144` tripped push protection → replaced with `<your-slack-webhook-url>`, rewrote the two unpushed commits. No real secret.

## 12. Persisted Market Snapshots + Scheduled Refresh (`bf0ac04`)

Fetchers (`yield_curve`, `fx_rates`) were memory-only with flagged fallbacks; nothing scheduled, stored, or observable.

- `backend/app/models/market.py` — `MarketSnapshot` (source, ok/fallback/error, payload, record_count); migration `015_market_snapshots`, single head.
- `backend/app/services/market_refresh.py` — fetch both sources (cache bypassed), classify via `is_fallback`, 1 retry, never raises (error snapshots), prunes >30d.
- `automation_engine`: new hourly `market_refresh` automation (ops) — existing scheduler picks it up, no new infra.
- `GET /api/data-quality/snapshots` — latest per source + `age_seconds` + `stale` (>25h).
- Agent tool `market_snapshot` — runs can verify freshness before reasoning.
- `tests/test_market_refresh.py` (6 tests, fetchers stubbed): ok/fallback/error classification, pruning, endpoint, agent tool.

Verified: 60 passed (market + agent + projects + market-data + data-quality). Pushed `f0483ab..bf0ac04`. Left 11 unrelated dirty paths (personal tax packs, web terms) untouched.

## Files Changed (Agent + Market Sessions)

- `backend/app/agent/` — `__init__.py`, `tools.py`, `models.py`, `runner.py`, `builtin_tools.py`
- `backend/app/api/agent_api.py`, `backend/app/api/projects_api.py`, `backend/app/api/data_quality.py`
- `backend/app/models/project.py`, `backend/app/models/market.py`
- `backend/app/services/market_refresh.py`, `backend/app/services/automation_engine.py`
- `backend/alembic/versions/012_agent_runs.py`, `013_agent_step_approver.py`, `014_projects.py`, `015_market_snapshots.py`
- `backend/tests/test_agent.py`, `test_projects.py`, `test_market_refresh.py`
- `frontend/src/pages/AgentRunsPage.tsx`, `frontend/src/api/index.ts`, `frontend/src/types/index.ts`, `frontend/src/App.tsx`
- `912.md` — this log

---

## 8. Codebase Mapping (Quantive orientation)

Explored via subagents (backend / frontend / quant-core):
- `backend/app/main.py:1` — FastAPI monolith, dual-mounted router (`/api/v1` + legacy), ~10 middleware layers, Jinja pages + `/static`, lifespan creates billing/discovery/automation tables + starts schedulers.
- `backend/app/api/__init__.py:1` — ~127 route modules aggregated; `config.py` (pydantic-settings, SQLite dev) + `database.py` (WAL+FK, org RLS context for Postgres).
- `frontend/src/App.tsx:1` — React 19 + Vite 8 + Tailwind 4, Context-only state (no zustand; react-query installed but unused), recharts, Electron shell (`electron.cjs` spawns uvicorn).
- `quantive/` — pure-python core: `orchestration.py:run_full_job()`, `solvers/` (milp/highs/annealing/qubo), `scenarios/`, `portfolio/optimizer.py`, `objectives/spec.py`. Backend wraps it (no math duplication) except `quantum/` + `simulation/` helpers.
- `run.py:1` — one-command launcher (builds `frontend/dist` if missing, starts uvicorn).

## 9. Quantive Personal — New Product Scaffold (same app, separate data)

Decision: completely new product from same enterprise — shared shell/billing/auth, **different databases, never mixed**.
- `backend/app/personal/__init__.py` — isolation contract doc.
- `backend/app/personal/database.py` — separate engine `quantive_personal.db` (`PERSONAL_DATABASE_URL`, `PersonalBase`, `get_personal_db`).
- `backend/app/personal/models.py` — `PersonalProfile`, `ProfileFact` (structured memory w/ source/confidence), `PersonalOpportunity` (relevance only, never savings), `PersonalTask`, `PersonalDocument` (metadata only), `TaxRule` (versioned juris×year), `PersonalAudit` (product-scoped).
- `backend/app/personal/{schemas,onboarding_questions (24Q adaptive),scoring (readiness only),opportunity_engine (deterministic, rule-cited)}.py`.
- `backend/app/personal/api.py` — 18 routes under `/api/personal` (score, onboarding, profile facts, opportunities, tasks, documents, intelligence, report, billing, gov-insights, checkout).
- Wiring: `config.py:31` (`PERSONAL_DATABASE_URL`), `api/__init__.py:140` (personal router), `main.py:232` (`PersonalBase.metadata.create_all` on lifespan).
- Frontend: `frontend/src/personal/` — `api.ts`, `PersonalLayout.tsx`, `personal.css`, 9 pages (Dashboard, Onboarding, Profile, Opportunities, Documents, Intelligence, GovInsights, Reports, Pricing); routes in `App.tsx:534-549` + nav entry.
- Product rules enforced: no fabricated deductions/savings/rules; uncertain = Potentially relevant / Needs verification; user owns profile (confirm/edit/remove).

## 10. Landing / Qubo / TOS / Government / Businesses (SPA)

- `frontend/src/pages/LandingPage.tsx` — rewrite: Government + Businesses entry cards, Qubo privacy strip, Personal 2k/5k/10k strip, TOS link.
- New: `QuboPage.tsx` (gets aggregates / never names+conversations), `GovernmentPage.tsx`, `BusinessPage.tsx`, `TermsPage.tsx` (6-section TOS); routes `/qubo /terms /government /business` in `App.tsx:330-341`.
- `backend/app/main.py:37` — added `/qubo /terms /government /business` to `PUBLIC_PATHS` (were 303 → /login).

## 11. Personal Plans: $2K Starter / $5K / $10K Sovereign

- `backend/app/billing.py:29` — `PERSONAL_2K` ($2k: 3 opps, 10 docs, 20 asks/mo, no gov, summary-only) + `PERSONAL_10K` ($10k: unlimited + `personal_gov_access` + full report).
- Enforcement in `personal/api.py`: fail-closed Starter defaults when no sub (`_personal_plan`), opps slice, docs cap (403), asks/month cap via audit count, `GET /gov-insights` (403 without Sovereign), `POST /billing/checkout` (Personal tiers only, absolute Stripe URLs — fixed relative-URL bug), `GET /billing` returns plans + `gov_access`.
- `opportunity_engine.py` — Sovereign extras (`Qubo trend alignment`, `Sovereign insight briefing`).
- Frontend: `PricingPage.tsx` (checkout + dev-mode confirm), `GovInsightsPage.tsx` (lock screen vs trends), plan-aware Dashboard/Opportunities/Intelligence/Documents/Reports (caps, locked teasers, upgrade links).

## 12. Worldwide Tax Packs (US / BD / MX / GEN)

Finding: detector ignored country — US and Bangladesh users got identical generic answers.
- `backend/app/personal/tax_packs.py` (new) — `PACKS[(juris,year)]`: US-2026 (7: mortgage/Schedule A, SALT cap flagged volatile, student-loan, medical AGI floor, 401k/IRA, charitable, home office), BD-2026 (4: investment rebate, HRA, savings instruments, rental deductions), MX-2026, GEN fallback. Honesty contract: every summary carries verify-against-authority language (test-enforced).
- `opportunity_engine.py` — rewritten `detect()` jurisdiction-aware (`_juris` from `country` fact, `_ref` prefers pack match else GEN); new branches: student-loan, SALT (US), HRA/rebate/savings/rental (BD), charitable (US medium).
- `onboarding_questions.py` — country options +BD; `api.py` intelligence `rule_refs` derived from detected opps (was hardcoded GEN), report gains `jurisdiction` + fallback note.

## 13. Real Landing `web/` (static, was untouched by §10)

Finding: actual landing is standalone `web/index.html` (+`web/dist/` mirror), not the SPA route.
- `web/index.html` — nav now Government/Businesses/Qubo/Pricing/Terms; hero = 2 buttons (For Government → `#government`, For Businesses → `#business`); new `#business` section (workspace + Personal + Sovereign cards); new `#qubo` section (Patterns-not-people: gets vs never-gets cards); new `#pricing-personal` block (Starter $2k / Personal $5k featured / Sovereign $10k → static `personal/pricing.html`); footer Terms → `terms.html`.
- `web/terms.html` (new) — full TOS in matching dark design, mirrored to `web/dist/terms.html`; `web/dist/index.html` re-synced.

## 14. Static Personal App `web/personal/` (no localhost needed)

Full client-side port — works from `file://`, zero server:
- `app.js` — store (localStorage `qp_state_v1`), 24Q adaptive flow, jurisdiction detector, score, Starter caps, plan switching; `style.css` (qp design tokens).
- 9 shells: `index/onboarding/profile/opportunities/documents/intelligence/gov/reports/pricing.html` (+ nav, demo banner, honest "demo billing" note).
- Verified: `node --check` clean; engine smoke in Node (US → mortgage+charitable+SALT; BD → HRA+rebate; other → GEN-only; score + adaptive skip correct); all shells reference `style.css`+`app.js`; mirrored to `web/dist/personal/`.
- Remaining `localhost:8000` links in `web/index.html`: only Sign in / Register / consultation / demo portfolio (real auth + live DB — cannot be static without faking auth).

## 15. Issue Sweep + Fixes

Fixed:
- Marketing pages login-walled (§10, `main.py:37`).
- `.env.example` — added 8 `STRIPE_PERSONAL_*` price IDs + `PERSONAL_DATABASE_URL` docs.
- No Personal backend tests → `backend/tests/test_personal.py` (14 tests: 24Q baseline, adaptive skip, score bounds, fail-closed defaults, limit boundaries, tier pricing, verify-gate on all rules, US/BD/GEN-only/MX refs, gov 403, checkout 422). Temp-DB isolated.
- Personal checkout relative-URL Stripe bug (§11).
- Static mortgage-ref naming mismatch (`GEN-2026-mortgage-interest` alias added in `web/personal/app.js`).
Pre-existing, left alone:
- Sovereign dev-DB drift: `quantive.db` lacks `users.email_verified` → live registration 500s (needs Alembic migration; Personal unaffected).
- `regex=` → `pattern=` deprecations (`transparency_index.py:31`, `immutable_audit_api.py:184`); class-based Pydantic config warnings.
- Personal bounces to `/login` without session (consistent with rest of app; no anonymous trial).
- oxlint 0 errors (372 warnings, pre-existing style incl. fetch-on-mount).

## Files Changed (Sessions 912–913 additions)

Backend:
- `backend/app/personal/` (new: `__init__, database, models, schemas, onboarding_questions, scoring, opportunity_engine, tax_packs, api`)
- `backend/app/{config,billing}.py` — Personal DB URL + 3 Personal tiers
- `backend/app/api/__init__.py` — personal router; `backend/app/api/billing_routes.py` — tier doc
- `backend/app/main.py` — Personal tables lifespan + public marketing paths
- `backend/tests/test_personal.py` (new, 14 tests)
Frontend (SPA):
- `frontend/src/personal/` (new: `api.ts`, `PersonalLayout.tsx`, `personal.css`, 9 pages)
- `frontend/src/{App.tsx,main.tsx}` — Personal + Qubo/Terms/Gov/Business routes
- `frontend/src/pages/{LandingPage,QuboPage,TermsPage,GovernmentPage,BusinessPage}.tsx`
Static site:
- `web/index.html` (+`web/dist/` mirror) — buttons, business/qubo sections, Personal pricing
- `web/terms.html` (+dist) — TOS; `web/personal/` (+dist, 11 files) — server-free app
- `.env.example` — Personal/Stripe docs; `912.md` — this log

## Verification Summary (updated)

- Backend personal: 14 passed; smoke: 23 passed (twice, incl. after tax-pack rewrite)
- Frontend vitest: 973 passed (45 files); `tsc --noEmit`: clean; oxlint: 0 errors
- Static engine: Node smoke (US/BD/GEN/score/adaptive) correct; 9/9 shells wired; all anchors + terms links resolve in `web/` and `web/dist/`
- Direct API checks: score/opps/report/billing/gov-gate-403/checkout-422 verified; smoke rows cleaned from dev DB
