"""Quantive agent package — controlled execution substrate.

Not a terminal clone: a typed tool registry + DB-backed plan→execute→verify
loop over Quantive's own domain (portfolios, optimizations, risk, data).
"""
